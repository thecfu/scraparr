name: Pylint

on:
  pull_request:
    paths:
      - '**.py'
      - 'uv.lock'
      - 'pyproject.toml'
      - '**/requirements.txt'
  workflow_dispatch:

jobs:
  lint:
    uses: ./.github/workflows/lint.yml
