name: Pylint

on:
  pull_request:
    paths:
      - '**.py'
      - 'uv.lock'
      - 'pyproject.toml'
      - '**/requirements.txt'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    uses: ./.forgejo/workflows/lint.yml
